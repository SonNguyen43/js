// let drawPoint = (a, b, c, d, e, f) {}

// let drawPoint = (point: {x: number, y: number}) => {}

interface Point{
    x: number,
    y: number
}
let drawPoint = (point: Point) => {}

drawPoint({x: 1, y:2})